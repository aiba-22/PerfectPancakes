- content_for(:title, 'マイレシピ一覧')
body.bg-secondary
  .container.padding_100
    .row
      button.btn.btn-primary[type="button" data-toggle="modal" data-target="#modalForm"]
        .m-1
          = t('.title_and_photo')
      = render 'shared/modal', { recipe: @recipe }

    .row
      ul.recipe_ul
        - @recipes_pagenate.each do |recipe|
          li.recipe_li.list-unstyled
            =link_to recipe_path(recipe.id)
              = image_tag recipe.image_url, class: 'recipe_img'
    .row
      = paginate @recipes_pagenate
