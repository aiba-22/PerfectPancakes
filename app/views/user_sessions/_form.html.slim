= form_with url: user_sessions_path, method: :post, local: true  do |f|
  .container.padding_100
    .row
      .text-center
        = f.label :email, User.human_attribute_name(:email)
    .row.pb-3
      .col-10.offset-1
          = f.text_field :email, class: 'form-control'
    .row
      .text-center
        = f.label :password, User.human_attribute_name(:password)
      .col-10.offset-1
        = f.text_field :password, type: 'password', class: 'form-control'
    .row.pt-5
      .col-4.offset-4
        = f.submit "ログイン", class: 'btn btn-primary'
    .row.pt-5
      .col
      = link_to  "パスワードを忘れた方はこちら", new_password_reset_path, class: 'text-dark'
